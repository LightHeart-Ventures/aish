{
  "mcpServers": {
    "signoz": {
      "type": "http",
      "url": "${SIGNOZ_MCP_URL}",
      "headers": {
        "SIGNOZ-API-KEY": "${profile:signoz}"
      },
      "_comment": "The agent-facing half. The signoz MCP server exposes signoz_search_logs / signoz_search_traces / signoz_aggregate_logs etc. If these tools are ALREADY registered globally (via ~/.aish/.mcp.json), this file is redundant — the plugin detects availability at on_init and skips setup. See README 'SigNoz MCP setup'."
    }
  }
}
